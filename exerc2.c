#include <stdio.h>
#include <stdlib.h>

int negativos (int n, float *vet);
int main () {
    int n,i;
    float vet;
    printf("Digite o tamanho do vetor: ");
    scanf("%d", &n);
    printf("Digite os numeros reias: ");
    vet=(float*)malloc(n*sizeof(float));
    for (i = 0; i < n; n++)
    {
        /* code */
    }
    
    }
